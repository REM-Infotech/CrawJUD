# common/exceptions/validacao.py

## Propósito

Módulo para controle de exceptions de validações de valores.

## Classe: `ValidacaoStringError`

Exception de erro de validação de string.

**Herda de:** `ValueError`

### Atributos

- `message` (str)
- `exception` (Exception)

### Métodos

#### `__init__()`

Inicializa exception de validação.

**Parâmetros:**

- `self` (Any)
- `message` (str)

