# common/exceptions/_file.py

## Propósito

Módulo do sistema CrawJUD.

## Dependências Principais

- `backend.common.exceptions`

## Classe: `ArquivoNaoEncontradoError`

**Herda de:** `BaseCrawJUDError`

### Métodos

#### `__init__()`

**Parâmetros:**

- `self` (Any)
- `caminho` (str)
- `message` (str)
- `exc` (Exception | None)

#### `__str__()`

**Parâmetros:**

- `self` (Any)

**Retorna:** str

#### `__repr__()`

**Parâmetros:**

- `self` (Any)

**Retorna:** str

#### `__getstate__()`

**Parâmetros:**

- `self` (Any)

**Retorna:** dict[str, str]

#### `__setstate__()`

**Parâmetros:**

- `self` (Any)
- `state` (AnyType)

