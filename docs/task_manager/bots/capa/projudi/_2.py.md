# task_manager/bots/capa/projudi/_2.py

## Propósito

Módulo do sistema CrawJUD.

## Dependências Principais

- `backend.controllers.projudi`
- `backend.interfaces.projudi`
- `backend.resources.elements`
- `bs4`
- `selenium.webdriver.common.by`
- `selenium.webdriver.support`

## Constantes

- `TDS_MAX_SIZE`

## Classe: `SegundaInstancia`

**Herda de:** `ProjudiBot`

### Métodos

#### `_informacoes_gerais_segundo_grau()`

**Parâmetros:**

- `self` (Any)

#### `_info_processual_segundo_grau()`

**Parâmetros:**

- `self` (Any)

**Retorna:** dict[str, str]

#### `_partes_segundo_grau()`

**Parâmetros:**

- `self` (Any)
- `numero_processo` (str)

**Retorna:** tuple[list[PartesProjudiDict], list[RepresentantesProjudiDict]]

#### `_partes_extract_segundo_grau()`

Extraia informações das partes do processo na tabela do Projudi.

Returns:
    tuple: advogados e partes

**Parâmetros:**

- `cls` (Any)
- `html` (str)
- `processo` (str)

**Retorna:** tuple[list[PartesProjudiDict], list[RepresentantesProjudiDict]]

## Função: `limpa_campo()`

**Parâmetros:**

- `valor` (str)

**Retorna:** str

### Exemplo de Uso

```python
resultado = limpa_campo(valor)
```

