# Documentação do backend/config/settings.yaml

## Propósito

Arquivo de configuração principal do backend. Define parâmetros e variáveis de ambiente.

## Exemplo de uso

O arquivo é lido automaticamente pelo sistema de configuração.
