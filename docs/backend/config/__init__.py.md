# Documentação do backend/config/**init**.py

## Propósito

Arquivo de inicialização do pacote `config` dentro do backend. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.config
```
