# Documentação do backend/types_app/**init**.py

## Propósito

Arquivo de inicialização do pacote `types_app` dentro do backend. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.types_app
```
