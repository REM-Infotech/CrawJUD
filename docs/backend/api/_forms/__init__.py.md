# Documentação do backend/api/\_forms/**init**.py

## Propósito

Arquivo de inicialização do subpacote `_forms` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api._forms
```
