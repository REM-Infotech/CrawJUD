# Documentação do backend/api/resources/**init**.py

## Propósito

Arquivo de inicialização do subpacote `resources` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api.resources
```
