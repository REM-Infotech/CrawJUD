# Documentação do backend/api/constants/**init**.py

## Propósito

Arquivo de inicialização do subpacote `constants` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api.constants
```
