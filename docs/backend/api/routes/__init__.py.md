# Documentação do backend/api/routes/**init**.py

## Propósito

Arquivo de inicialização do subpacote `routes` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api.routes
```
