# Documentação do backend/api/decorators/**init**.py

## Propósito

Arquivo de inicialização do subpacote `decorators` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api.decorators
```
