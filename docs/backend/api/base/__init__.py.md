# Documentação do backend/api/base/**init**.py

## Propósito

Arquivo de inicialização do subpacote `base` dentro de `api`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.api.base
```
