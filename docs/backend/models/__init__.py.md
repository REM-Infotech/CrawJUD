# Documentação do backend/models/**init**.py

## Propósito

Arquivo de inicialização do pacote `models` dentro do backend. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.models
```
