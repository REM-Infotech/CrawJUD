# Documentação do backend/interfaces/elaw/**init**.py

## Propósito

Arquivo de inicialização do subpacote `elaw` dentro de `interfaces`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.interfaces.elaw
```
