# Documentação do backend/interfaces/payloads/**init**.py

## Propósito

Arquivo de inicialização do subpacote `payloads` dentro de `interfaces`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.interfaces.payloads
```
