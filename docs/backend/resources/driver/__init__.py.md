# Documentação do backend/resources/driver/**init**.py

## Propósito

Arquivo de inicialização do subpacote `driver` dentro de `resources`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.resources.driver
```
