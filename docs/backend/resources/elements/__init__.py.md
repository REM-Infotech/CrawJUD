# Documentação do backend/resources/elements/**init**.py

## Propósito

Arquivo de inicialização do subpacote `elements` dentro de `resources`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.resources.elements
```
