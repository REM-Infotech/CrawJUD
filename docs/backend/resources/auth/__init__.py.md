# Documentação do backend/resources/auth/**init**.py

## Propósito

Arquivo de inicialização do subpacote `auth` dentro de `resources`. Permite que o diretório seja tratado como um módulo Python.

## Exemplo de uso

```python
import backend.resources.auth
```
